# §6 Scenario Coverage Matrix

把原始提案的 5 个对照场景图明确落到本 PR 的改动上。

| 提案   | 场景描述                 | 当前痛点                                                         | 涉及组件                                                                       | 改动                                                                                                                                                                      | 验收                                                                        |
| ------ | ------------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 图 4   | 长 Shell 工具调用 border | 整块 round border + 内嵌 ansi output 多行边框包裹                | `ToolGroupMessage.tsx`, `ToolMessage.tsx`（shell 分支用 `AnsiOutputRenderer`） | §3.3 去 ToolGroupMessage 外层 border；`AnsiOutputRenderer` 自身仍保留必要 frame（它表达 PTY 屏幕语义，不能去）                                                            | shell 长命令上下文紧凑、左缘对齐、仅末尾 `⎿  exit 0 · 6.3s`                 |
| 图 5   | 代码生成工具 border      | edit/write 工具结果是 DiffRenderer，外层 + 内层 双 border        | `ToolGroupMessage.tsx`, `DiffRenderer`                                         | §3.3 去外层 border。DiffRenderer 自身**保留**（它的 `borderStyle="round"` 表达 diff 边界语义，去掉后 +/- 行视觉混乱）。文档 §02 明示此决策。                              | 编辑工具仅 diff 自身有 frame，前后无 chrome 包裹                            |
| 图 7   | 表格内代码高亮           | inline code 在 table cell 中走 highlight.js，五彩切碎对齐        | `InlineMarkdownRenderer`, `TableRenderer`                                      | §3.6 `InlineMarkdownRenderer` 新增 `inTable` prop，在 table 中跳过 highlight，回退单色 secondary                                                                          | 表格中 ` `code` ` 单色等宽，cell 对齐稳定                                   |
| 图 9   | 文件读取错误场景         | error 与 retry 的边界靠 border 区分；间距过松                    | `ErrorMessage`, `RetryCountdownMessage`, `HistoryItemDisplay` marginTop        | §3.2 marginTop 重定义；`ErrorMessage`/`RetryCountdownMessage` 不归入 TOP_MARGIN_TYPES → marginTop=0。**新增**：error/retry 也用 `⏺` warning 色前缀，与 tool 行视觉同源。 | 文件读 + 错误 + 重试 3 行紧贴成一个 logical block，颜色区分而非 border 区分 |
| 图 10  | 简单项目检查场景多块间距 | `about/stats/help` 等 "section item" 内部多块 1 行间距，整体过松 | `AboutBox`, `StatsDisplay`, `Help`, `ContextUsage` 等                          | §3.2 把这些保留外层 `marginTop=1`（section boundary），但**内部多 block 间距全部压成 0**（删除 inner Boxes 的 `marginTop=1`）。逐组件 audit 见 12-call-site-audit         | section 内部紧凑、外层与上下文 1 行分隔                                     |
| 图 11  | Diff 编辑器场景          | DiffRenderer 上下文紧贴差、`+/-` 行内字符高亮溢出                | `DiffRenderer`                                                                 | §3.3 去 ToolGroupMessage 外层 border；DiffRenderer 自身 frame **保留**（语义性）；inline diff 字符不变                                                                    | edit 工具上下文紧凑，diff 仍可读                                            |
| 图 12  | 长流式输出场景           | 长 markdown 在 streaming 中持续 reflow + 行间空白多              | `AssistantMessage`, `MarkdownDisplay`, `MainContent` VP 渲染                   | §3.2 `gemini` / `gemini_content` marginTop=0（已是 0 for `gemini_content`，gemini 改为 0）；§3.5 turn 结束追加 `⏱`；VP 路径已能流畅滚动长流                              | 长回复内文行间无多余空行，结尾紧跟 `⏱`                                     |
| 图 1   | 思考过程对比             | qwen 默认展示思考链 `✦` 占大量行                                 | §3.1 全文                                                                      | thoughts 渲染层 gate；live `✻ Thinking…` pulse；transcript overlay 可看完整                                                                                               | 默认 turn 无 thoughts；Ctrl+O 可见                                          |
| 图 2/3 | 简单问答 / 文件列表 间距 | user→assistant 2 行间距 + 块间松                                 | §3.2 marginTop 重定义                                                          | Q&A 间 1 行；turn 内紧贴                                                                                                                                                  | 简单问答总行数减少 40-50%                                                   |
| 图 6   | SubAgent 展示对比        | qwen 当前 single-line scrollback summary，缺组聚合               | §3.4 全文                                                                      | 多 subagent 聚合 header；单 subagent 两行结构化；live panel ≥2 加 group                                                                                                   | 多 subagent batch 一眼看出 N completed / X failed                           |
| 图 8   | 首屏 Banner              | 各 CLI 品牌                                                      | **out of scope (本 PR 不动 banner)**                                           | —                                                                                                                                                                         | —                                                                           |
