name: 'Qwen Issue Triage'

on:
  issues:
    types: ['opened']
  issue_comment:
    types: ['created']
  workflow_dispatch:
    inputs:
      number:
        description: 'Issue number to triage'
        required: true
        type: 'number'

permissions:
  contents: 'read'
  issues: 'write'

jobs:
  triage:
    timeout-minutes: 30
    concurrency:
      group: '${{ github.workflow }}-${{ github.event.issue.number || github.event.inputs.number }}'
      cancel-in-progress: >-
        ${{
          github.event_name == 'issues' ||
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'issue_comment' &&
           startsWith(github.event.comment.body, '@qwen-code /triage') &&
           (github.event.comment.author_association == 'OWNER' ||
            github.event.comment.author_association == 'MEMBER' ||
            github.event.comment.author_association == 'COLLABORATOR'))
        }}
    runs-on: 'ubuntu-latest'
    if: >-
      github.repository == 'QwenLM/qwen-code' &&
      !github.event.issue.pull_request && (
        github.event_name == 'issues' ||
        github.event_name == 'workflow_dispatch' ||
        (github.event_name == 'issue_comment' &&
         startsWith(github.event.comment.body, '@qwen-code /triage') &&
         (github.event.comment.author_association == 'OWNER' ||
          github.event.comment.author_association == 'MEMBER' ||
          github.event.comment.author_association == 'COLLABORATOR'))
      )
    steps:
      - name: 'Checkout repo'
        uses: 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd' # v6.0.2
        with:
          token: '${{ secrets.GITHUB_TOKEN }}'

      - name: 'Resolve target number'
        id: 'resolve'
        env:
          EVENT_NAME: '${{ github.event_name }}'
          INPUT_NUMBER: '${{ github.event.inputs.number }}'
          ISSUE_NUMBER: '${{ github.event.issue.number }}'
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "number=$INPUT_NUMBER" >> "$GITHUB_OUTPUT"
          else
            echo "number=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
          fi

      - name: 'Run Qwen Issue Triage'
        uses: 'QwenLM/qwen-code-action@5fd6818d04d64e87d255ee4d5f77995e32fbf4c2'
        env:
          GITHUB_TOKEN: '${{ secrets.QWEN_CODE_BOT_TOKEN || secrets.CI_BOT_PAT }}'
          GH_TOKEN: '${{ secrets.QWEN_CODE_BOT_TOKEN || secrets.CI_BOT_PAT }}'
          REPOSITORY: '${{ github.repository }}'
        with:
          OPENAI_API_KEY: '${{ secrets.OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_PR_REVIEW_MODEL }}'
          settings_json: |-
            {
              "coreTools": [
                "run_shell_command",
                "write_file",
                "read_file",
                "grep_search",
                "glob",
                "agent",
                "enter_worktree",
                "exit_worktree"
              ],
              "sandbox": false
            }
          prompt: '/triage ${{ steps.resolve.outputs.number }} --repo ${{ github.repository }}'
