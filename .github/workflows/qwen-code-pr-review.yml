name: 'Qwen Pull Request Review'

on:
  pull_request_target:
    types: ['opened', 'reopened', 'ready_for_review', 'synchronize', 'edited']
  issue_comment:
    types: ['created', 'edited']
  pull_request_review_comment:
    types: ['created', 'edited']
  pull_request_review:
    types: ['submitted']
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to review'
        required: true
        type: 'number'
      review_mode:
        description: 'Run without posting comments, or publish a PR summary comment'
        required: true
        default: 'dry-run'
        type: 'choice'
        options:
          - 'dry-run'
          - 'comment'
      additional_instructions:
        description: 'Optional maintainer focus for this review'
        required: false
        type: 'string'

jobs:
  review-pr:
    if: |-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request_target' &&
       (github.event.action == 'opened' || github.event.action == 'reopened' || github.event.action == 'ready_for_review' || github.event.action == 'synchronize' || (github.event.action == 'edited' && (github.event.changes.body || github.event.changes.title))) &&
       (github.event.pull_request.author_association == 'OWNER' ||
        github.event.pull_request.author_association == 'MEMBER' ||
        github.event.pull_request.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request &&
        (contains(github.event.comment.body, '@qwen /review') || contains(github.event.comment.body, '@qwen /design-gate')) &&
        (github.event.comment.author_association == 'OWNER' ||
         github.event.comment.author_association == 'MEMBER' ||
         github.event.comment.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'pull_request_review_comment' &&
        (contains(github.event.comment.body, '@qwen /review') || contains(github.event.comment.body, '@qwen /design-gate')) &&
        (github.event.comment.author_association == 'OWNER' ||
         github.event.comment.author_association == 'MEMBER' ||
         github.event.comment.author_association == 'COLLABORATOR')) ||
      (github.event_name == 'pull_request_review' &&
        (contains(github.event.review.body, '@qwen /review') || contains(github.event.review.body, '@qwen /design-gate')) &&
        (github.event.review.author_association == 'OWNER' ||
         github.event.review.author_association == 'MEMBER' ||
         github.event.review.author_association == 'COLLABORATOR'))
    concurrency:
      group: 'qwen-pr-review-${{ github.event.issue.number || github.event.pull_request.number || github.event.inputs.pr_number }}'
      cancel-in-progress: true
    timeout-minutes: 30
    runs-on: 'ubuntu-latest'
    permissions:
      checks: 'read'
      contents: 'read'
      pull-requests: 'read'
      issues: 'write'
      statuses: 'read'
    env:
      GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
      OPENAI_MODEL: '${{ vars.QWEN_PR_REVIEW_MODEL }}'
      QWEN_PR_REVIEW_MAX_CHANGED_LINES: "${{ vars.QWEN_PR_REVIEW_MAX_CHANGED_LINES || '1500' }}"
    steps:
      - name: 'Checkout base branch'
        if: |-
          env.ACT != 'true'
        uses: 'actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683' # v4.2.2
        with:
          token: '${{ secrets.GITHUB_TOKEN }}'
          ref: 'main'
          fetch-depth: 0

      - name: 'Install GitHub CLI for local act'
        if: |-
          env.ACT == 'true'
        run: |-
          set -euo pipefail
          if command -v gh >/dev/null 2>&1; then
            exit 0
          fi

          sudo mkdir -p -m 755 /etc/apt/keyrings
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg |
            sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg >/dev/null
          sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" |
            sudo tee /etc/apt/sources.list.d/github-cli.list >/dev/null
          sudo apt-get update
          sudo apt-get install -y gh jq

      - name: 'Resolve PR context'
        id: 'pr'
        env:
          WORKFLOW_PR_NUMBER: '${{ github.event.inputs.pr_number }}'
          WORKFLOW_REVIEW_MODE: '${{ github.event.inputs.review_mode }}'
          WORKFLOW_ADDITIONAL_INSTRUCTIONS: '${{ github.event.inputs.additional_instructions }}'
        run: |-
          set -euo pipefail
          node .github/scripts/resolve-review-context.mjs

      - name: 'Check PR size'
        id: 'size'
        if: |-
          steps.pr.outputs.should_run_review == 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          REVIEW_MODE: '${{ steps.pr.outputs.review_mode }}'
          SHOULD_COMMENT: '${{ steps.pr.outputs.should_comment }}'
        run: |-
          set -euo pipefail

          write_output() {
            echo "$1" >> "$GITHUB_OUTPUT"
          }

          if [ -z "${OPENAI_MODEL:-}" ]; then
            echo "::error::Repository variable QWEN_PR_REVIEW_MODEL is required for this workflow (maps to env var OPENAI_MODEL)."
            exit 1
          fi

          if ! printf '%s' "$QWEN_PR_REVIEW_MAX_CHANGED_LINES" | grep -Eq '^[0-9]+$'; then
            echo "::error::QWEN_PR_REVIEW_MAX_CHANGED_LINES must be an integer."
            exit 1
          fi

          if [ "$QWEN_PR_REVIEW_MAX_CHANGED_LINES" -lt 100 ] || [ "$QWEN_PR_REVIEW_MAX_CHANGED_LINES" -gt 50000 ]; then
            echo "::error::QWEN_PR_REVIEW_MAX_CHANGED_LINES must be between 100 and 50000 (got $QWEN_PR_REVIEW_MAX_CHANGED_LINES)."
            exit 1
          fi

          pr_json="$(gh pr view "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --json additions,deletions,changedFiles,title,baseRefName,baseRefOid,headRefName,headRefOid,isCrossRepository,headRepositoryOwner,headRepository)"
          additions="$(jq -r '.additions' <<< "$pr_json")"
          deletions="$(jq -r '.deletions' <<< "$pr_json")"
          changed_files="$(jq -r '.changedFiles' <<< "$pr_json")"
          title="$(jq -r '.title' <<< "$pr_json")"
          base_ref="$(jq -r '.baseRefName' <<< "$pr_json")"
          base_sha="$(jq -r '.baseRefOid // ""' <<< "$pr_json")"
          head_ref="$(jq -r '.headRefName' <<< "$pr_json")"
          head_sha="$(jq -r '.headRefOid // ""' <<< "$pr_json")"
          is_cross_repository="$(jq -r '.isCrossRepository' <<< "$pr_json")"
          head_owner="$(jq -r '.headRepositoryOwner.login // ""' <<< "$pr_json")"
          head_repo="$(jq -r '.headRepository.name // ""' <<< "$pr_json")"
          changed_lines=$((additions + deletions))

          # Reject empty head_sha / base_sha early. Both are queried via
          # gh pr view; an empty value almost always means gh pr view
          # partially failed.
          if [ -z "$head_sha" ]; then
            echo "::error::Could not resolve PR head SHA via gh pr view --json headRefOid."
            exit 1
          fi
          if [ -z "$base_sha" ]; then
            echo "::error::Could not resolve PR base SHA via gh pr view --json baseRefOid."
            exit 1
          fi

          # Compute the merge-base SHA, i.e., the commit where the PR's
          # history forks from the base branch. The merge base is what the
          # cache key must be scoped to, NOT baseRefOid.
          #
          # baseRefOid is the current tip of the base ref (e.g., main HEAD).
          # That value can stay fixed across reviews even when the PR
          # merges base into itself: if the author clicks "Update branch"
          # while base hasn't moved, baseRefOid is unchanged but the PR's
          # history now incorporates base's commits. A cache restored under
          # baseRefOid would then let the bundled /review skill compute
          # `git diff <oldHead>..<newHead>` across upstream commits the PR
          # did not author. The merge base, in contrast, advances whenever
          # base content enters the PR (Update branch, rebase onto newer
          # base, or base retarget), which is exactly the boundary we
          # want to invalidate the cache on.
          #
          # Skip merge-base for cross-repository PRs: the compare endpoint
          # can fail to resolve fork SHAs in the base repo's namespace,
          # which would abort this step (set -euo pipefail) before the
          # is_cross_repository guard below has a chance to post the
          # fork-rejection comment. Forks never reach the cache restore /
          # save steps anyway because should_review is set to false.
          merge_base_sha=""
          if [ "$is_cross_repository" != "true" ]; then
            merge_base_sha="$(gh api \
              "repos/${GITHUB_REPOSITORY}/compare/${base_sha}...${head_sha}" \
              --jq '.merge_base_commit.sha // ""')"
            if [ -z "$merge_base_sha" ]; then
              echo "::error::Could not resolve PR merge base via gh api compare."
              exit 1
            fi
          fi

          write_output "changed_lines=$changed_lines"
          write_output "changed_files=$changed_files"
          write_output "head_sha=$head_sha"
          write_output "base_sha=$base_sha"
          write_output "merge_base_sha=$merge_base_sha"
          echo "Review target: PR #$PR_NUMBER"
          echo "Review title: $title"
          echo "Review branch: $base_ref ($base_sha) <- $head_owner/$head_repo:$head_ref ($head_sha)"
          if [ -n "$merge_base_sha" ]; then
            echo "Review merge base: $merge_base_sha"
          else
            echo "Review merge base: (skipped for cross-repository PR)"
          fi
          echo "Review scope: $changed_files files, +$additions/-$deletions ($changed_lines changed lines)"

          if [ "$is_cross_repository" = "true" ]; then
            write_output "should_review=false"
            cat > qwen-pr-review-fork-comment.md <<'EOF'
          Qwen Code automated PR review is disabled for cross-repository PRs because this workflow runs with review credentials and the bundled "/review" flow may install dependencies from the PR head.

          A maintainer can still review this PR manually, or copy trusted patches into a branch in this repository before requesting automated review.
          EOF
            if [ "$SHOULD_COMMENT" = "true" ] && [ "${ACT:-}" != "true" ]; then
              gh pr comment "$PR_NUMBER" \
                --repo "$GITHUB_REPOSITORY" \
                --body-file qwen-pr-review-fork-comment.md || true
            else
              {
                printf '### Qwen PR review dry run\n\n'
                cat qwen-pr-review-fork-comment.md
                printf "\nReview mode: \`%s\`; no PR comments were posted.\n" "$REVIEW_MODE"
              } >> "$GITHUB_STEP_SUMMARY"
            fi
            exit 0
          fi

          if [ "$changed_lines" -gt "$QWEN_PR_REVIEW_MAX_CHANGED_LINES" ]; then
            write_output "should_review=false"
            {
              printf 'This PR changes %s lines across %s files, which is above the current automated review threshold of %s changed lines.\n\n' \
                "$changed_lines" "$changed_files" "$QWEN_PR_REVIEW_MAX_CHANGED_LINES"
              printf 'Please consider splitting it into smaller, focused PRs before requesting a full Qwen Code review. Smaller PRs are easier to validate, easier to dogfood, and less likely to mix product direction, refactoring, and implementation details in one review.\n\n'
              printf "_Qwen Code PR review did not run a detailed code review for this oversized changeset. Model configured for review: \`%s\`._\n" \
                "$OPENAI_MODEL"
            } > qwen-pr-review-size-comment.md
            if [ "$SHOULD_COMMENT" = "true" ] && [ "${ACT:-}" != "true" ]; then
              gh pr comment "$PR_NUMBER" \
                --repo "$GITHUB_REPOSITORY" \
                --body-file qwen-pr-review-size-comment.md || true
            else
              {
                printf '### Qwen PR review dry run\n\n'
                cat qwen-pr-review-size-comment.md
                printf "\n\nReview mode: \`%s\`; no PR comments were posted.\n" "$REVIEW_MODE"
              } >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            write_output "should_review=true"
          fi

      - name: 'Generate PR shape'
        id: 'pr-shape'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.bypass_design_gate != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
        run: |-
          set -euo pipefail
          mkdir -p .qwen/tmp
          node .github/scripts/pr-shape.mjs \
            --repo "$GITHUB_REPOSITORY" \
            --pr "$PR_NUMBER" \
            --out ".qwen/tmp/qwen-review-pr-${PR_NUMBER}-shape.json"

      - name: 'Scan review history'
        id: 'history-scan'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.bypass_design_gate != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
        run: |-
          set -euo pipefail
          node .github/scripts/history-scan.mjs \
            --repo "$GITHUB_REPOSITORY" \
            --pr "$PR_NUMBER" \
            --shape ".qwen/tmp/qwen-review-pr-${PR_NUMBER}-shape.json" \
            --out ".qwen/tmp/qwen-review-pr-${PR_NUMBER}-history.json"

      - name: 'Run Design Gate'
        id: 'design-gate'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.bypass_design_gate != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          OPENAI_API_KEY: '${{ secrets.REVIEW_OPENAI_API_KEY }}'
          OPENAI_BASE_URL: '${{ secrets.REVIEW_OPENAI_BASE_URL }}'
          OPENAI_MODEL: '${{ vars.QWEN_PR_REVIEW_MODEL }}'
          QWEN_DESIGN_GATE_LLM: "${{ vars.QWEN_DESIGN_GATE_LLM || 'true' }}"
        run: |-
          set -euo pipefail

          gate_out=".qwen/tmp/qwen-review-pr-${PR_NUMBER}-design-gate.json"
          process_comment=".qwen/tmp/qwen-review-pr-${PR_NUMBER}-design-gate-comment.md"
          prompt_append=".qwen/tmp/qwen-review-pr-${PR_NUMBER}-design-gate-prompt.md"

          node .github/scripts/design-gate.mjs \
            --repo "$GITHUB_REPOSITORY" \
            --pr "$PR_NUMBER" \
            --shape ".qwen/tmp/qwen-review-pr-${PR_NUMBER}-shape.json" \
            --history ".qwen/tmp/qwen-review-pr-${PR_NUMBER}-history.json" \
            --out "$gate_out" \
            --process-comment-out "$process_comment" \
            --prompt-append-out "$prompt_append"

          status="$(jq -r '.status' "$gate_out")"
          # Guard against a malformed gate output (missing/renamed field
          # makes jq emit "null"). Fail loudly instead of silently
          # treating an unknown status as "not BLOCK" and proceeding.
          case "$status" in
            PASS | ADVISORY_ONLY | BLOCK) ;;
            *)
              echo "::error::Design Gate returned an invalid status: '$status'. Expected PASS, ADVISORY_ONLY, or BLOCK."
              exit 1
              ;;
          esac
          if [ "$status" = "BLOCK" ]; then
            should_review="false"
          else
            should_review="true"
          fi

          {
            echo "status=$status"
            echo "should_review=$should_review"
            echo "process_comment=$process_comment"
            echo "prompt_append=$prompt_append"
          } >> "$GITHUB_OUTPUT"

          {
            printf '### Qwen Design Gate\n\n'
            cat "$process_comment"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: 'Post Design Gate status comment'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.bypass_design_gate != 'true' &&
          (steps.design-gate.outputs.status == 'BLOCK' || steps.pr.outputs.gate_only == 'true') &&
          steps.pr.outputs.should_comment == 'true' &&
          env.ACT != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          PROCESS_COMMENT: '${{ steps.design-gate.outputs.process_comment }}'
        run: |-
          set -euo pipefail
          # Informational gate comment: a transient GitHub API failure
          # here must not fail the job and suppress the deep review.
          gh pr comment "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --body-file "$PROCESS_COMMENT" || true

      - name: 'Post Design Gate override audit'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.bypass_design_gate == 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          OVERRIDE_ACTOR: '${{ steps.pr.outputs.override_actor }}'
          OVERRIDE_REASON: '${{ steps.pr.outputs.override_reason }}'
          SHOULD_COMMENT: '${{ steps.pr.outputs.should_comment }}'
        run: |-
          set -euo pipefail
          {
            printf '## Qwen Design Gate: Maintainer Override\n\n'
            printf 'Design Gate was bypassed by @%s.\n\n' "$OVERRIDE_ACTOR"
            printf 'Reason: %s\n' "$OVERRIDE_REASON"
          } > qwen-design-gate-override-comment.md

          {
            printf '### Qwen Design Gate override\n\n'
            cat qwen-design-gate-override-comment.md
          } >> "$GITHUB_STEP_SUMMARY"

          if [ "$SHOULD_COMMENT" = "true" ] && [ "${ACT:-}" != "true" ]; then
            gh pr comment "$PR_NUMBER" \
              --repo "$GITHUB_REPOSITORY" \
              --body-file qwen-design-gate-override-comment.md || true
          fi

      - name: 'Prepare final review prompt'
        id: 'review-prompt'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.gate_only != 'true' &&
          (steps.pr.outputs.bypass_design_gate == 'true' || steps.design-gate.outputs.should_review == 'true')
        env:
          BASE_PROMPT: '${{ steps.pr.outputs.review_prompt }}'
          PROMPT_APPEND: '${{ steps.design-gate.outputs.prompt_append }}'
        run: |-
          set -euo pipefail

          delimiter="QWEN_FINAL_REVIEW_PROMPT_$(date +%s%N)"
          {
            echo "review_prompt<<$delimiter"
            printf '%s\n' "$BASE_PROMPT"
            if [ -n "${PROMPT_APPEND:-}" ] && [ -s "$PROMPT_APPEND" ]; then
              printf '\n'
              cat "$PROMPT_APPEND"
            fi
            echo "$delimiter"
          } >> "$GITHUB_OUTPUT"

      # Restore the bundled /review skill's per-PR cache only on synchronize.
      # The skill writes .qwen/review-cache/pr-<n>.json with the last reviewed
      # commit SHA so subsequent runs can scope review to the incremental
      # diff. Cache is intentionally NOT restored on opened/reopened (full
      # review) or comment / workflow_dispatch (manual re-review must not
      # short-circuit on "No new changes since last review"). See
      # docs/design/code-review/code-review-design.md §增量评审与缓存.
      #
      # Cache key uses merge_base_sha + head_sha (not baseRefOid). The
      # merge base advances whenever base content enters the PR — Update
      # branch, rebase onto newer base, or base retarget — which are
      # exactly the boundaries that should invalidate the cache. Pure
      # author pushes leave the merge base alone, so the restore-keys
      # prefix `qwen-review-<pr#>-<merge_base_sha>-` keeps matching and
      # bundled /review can still scope to the incremental diff.
      - name: 'Restore review cache'
        id: 'restore-cache'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.gate_only != 'true' &&
          (steps.pr.outputs.bypass_design_gate == 'true' || steps.design-gate.outputs.should_review == 'true') &&
          github.event_name == 'pull_request_target' &&
          github.event.action == 'synchronize'
        uses: 'actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830' # v4.3.0
        with:
          path: '.qwen/review-cache'
          key: 'qwen-review-${{ steps.pr.outputs.number }}-${{ steps.size.outputs.merge_base_sha }}-${{ steps.size.outputs.head_sha }}'
          restore-keys: |
            qwen-review-${{ steps.pr.outputs.number }}-${{ steps.size.outputs.merge_base_sha }}-

      - name: 'Run Qwen Code Review'
        id: 'review'
        if: |-
          steps.size.outputs.should_review == 'true' &&
          steps.pr.outputs.gate_only != 'true' &&
          (steps.pr.outputs.bypass_design_gate == 'true' || steps.design-gate.outputs.should_review == 'true')
        uses: 'QwenLM/qwen-code-action@a08dc886c2094312d6cf2df08ba5fd0437c53339' # main pinned on 2026-05-14
        with:
          openai_api_key: '${{ secrets.REVIEW_OPENAI_API_KEY }}'
          openai_base_url: '${{ secrets.REVIEW_OPENAI_BASE_URL }}'
          openai_model: '${{ vars.QWEN_PR_REVIEW_MODEL }}'
          prompt: '${{ steps.review-prompt.outputs.review_prompt }}'

      - name: 'Post dry-run summary'
        if: |-
          steps.review.outcome == 'success' &&
          steps.pr.outputs.should_comment != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
        run: |-
          {
            printf '### Qwen PR review dry run\n\n'
            printf 'Completed review for PR #%s without posting PR comments.\n' "$PR_NUMBER"
            printf '\nReview logs are available in this workflow run.\n'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: 'Post review summary comment'
        id: 'post-summary'
        if: |-
          steps.review.outcome == 'success' &&
          steps.pr.outputs.should_comment == 'true' &&
          env.ACT != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          REVIEW_SUMMARY: '${{ steps.review.outputs.summary }}'
        run: |-
          set -euo pipefail

          {
            printf '## Qwen Code Review\n\n'
            if [ -n "${REVIEW_SUMMARY:-}" ]; then
              printf '%s\n' "${REVIEW_SUMMARY:0:60000}"
              if [ "${#REVIEW_SUMMARY}" -gt 60000 ]; then
                printf '\n\n_Review summary was truncated. See the workflow logs for the full output._\n'
              fi
            else
              printf '_Qwen Code review completed, but no summary was captured. See the workflow logs for details._\n'
            fi
          } > qwen-pr-review-summary-comment.md

          gh pr comment "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --body-file qwen-pr-review-summary-comment.md

      # Check the exact cache key after the review comment is delivered.
      # lookup-only avoids downloading/restoring cache contents here; it only
      # prevents actions/cache/save from attempting to create a key that already
      # exists, which can happen on reruns of opened/reopened/ready_for_review
      # where the pre-review restore step intentionally did not run.
      - name: 'Check review cache key'
        id: 'cache-lookup'
        if: |-
          steps.review.outcome == 'success' &&
          steps.post-summary.outcome == 'success' &&
          github.event_name == 'pull_request_target' &&
          steps.size.outputs.head_sha != '' &&
          steps.size.outputs.merge_base_sha != ''
        uses: 'actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830' # v4.3.0
        with:
          path: '.qwen/review-cache'
          key: 'qwen-review-${{ steps.pr.outputs.number }}-${{ steps.size.outputs.merge_base_sha }}-${{ steps.size.outputs.head_sha }}'
          lookup-only: true

      # Save cache only AFTER the review summary comment was successfully
      # posted. Saving before publication would persist "this head was
      # reviewed" state even if `gh pr comment` later failed (rate-limit,
      # network, deleted PR, etc.) — the next synchronize would then
      # restore the cache, bundled /review would short-circuit on
      # "No new changes since last review" or scope to a tiny incremental
      # diff, and the findings that never reached the PR would be lost.
      # Gating on post-summary.outcome makes cache advancement track
      # comment delivery, not just model success. The lookup step above skips
      # duplicate exact-key saves on reruns.
      - name: 'Save review cache'
        # continue-on-error: the review summary is already published by
        # this point. A concurrent run can save the same key between the
        # lookup step and here, making actions/cache/save throw "Cache
        # already exists". That must not fail an otherwise successful
        # review (and must not trip the review-failure fallback).
        continue-on-error: true
        if: |-
          steps.review.outcome == 'success' &&
          steps.post-summary.outcome == 'success' &&
          github.event_name == 'pull_request_target' &&
          steps.size.outputs.head_sha != '' &&
          steps.size.outputs.merge_base_sha != '' &&
          steps.cache-lookup.outputs.cache-hit != 'true'
        uses: 'actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830' # v4.3.0
        with:
          path: '.qwen/review-cache'
          key: 'qwen-review-${{ steps.pr.outputs.number }}-${{ steps.size.outputs.merge_base_sha }}-${{ steps.size.outputs.head_sha }}'

      - name: 'Post fallback comment on review failure'
        if: |-
          failure() &&
          steps.review.conclusion == 'failure' &&
          steps.pr.outputs.should_comment == 'true' &&
          env.ACT != 'true'
        env:
          PR_NUMBER: '${{ steps.pr.outputs.number }}'
          RUN_URL: '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'
        run: |-
          set -euo pipefail

          {
            printf '_Qwen Code automated PR review did not complete successfully. See the workflow logs for details: %s_\n' "$RUN_URL"
            printf "\nThis is an automated message; please retry by commenting \`@qwen /review\` once the underlying issue is resolved.\n"
          } > qwen-pr-review-failure-comment.md
          gh pr comment "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --body-file qwen-pr-review-failure-comment.md || true
